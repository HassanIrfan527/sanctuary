[ids]
*

[main]
# CapsLock: hold = Super (Meta), tap = Esc.
# Everything else is handled by Hyprland — CapsLock+<key> == Super+<key>.
# All shortcuts live in ~/.config/hypr/keybinds.conf (single source of truth).
capslock = overload(meta, esc)
