# Portal & authentication
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = systemctl --user start hyprpolkitagent

# Hyprland plugins
exec-once = hyprpm reload -n

# Bar & notifications
exec-once = waybar
exec-once = swaync

# Night light
exec-once = hyprsunset -t 3800

# Core services
exec-once = swww-daemon
exec-once = hypridle
exec-once = swayosd-server

# Launcher backend + service
exec-once = systemctl --user start elephant.service
exec-once = walker --gapplication-service

# Keyring
exec-once = gnome-keyring-daemon --start --components=secrets

# GTK / icons / cursor — catppuccin mocha lavender
exec-once = gsettings set org.gnome.desktop.interface color-scheme 'prefer-dark'
exec-once = gsettings set org.gnome.desktop.interface gtk-theme 'catppuccin-mocha-lavender-standard+default'
exec-once = gsettings set org.gnome.desktop.interface icon-theme 'Cawnonical Mono-Glyph Icon Suite (Catppuccin Lavender)'
exec-once = gsettings set org.gnome.desktop.interface cursor-theme 'Drop-Purple'
exec-once = gsettings set org.gnome.desktop.interface cursor-size 24
exec-once = hyprctl setcursor Drop-Purple 24

# Key sounds
exec-once = ~/.config/hypr/scripts/keysounds-daemon.sh
