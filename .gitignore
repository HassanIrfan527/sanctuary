# Python
__pycache__/
*.py[cod]

# Editor/session backups — regenerated, not config
*.bak
*.gnome-backup

# Runtime theme state — rewritten by theme-switch.sh on every toggle
kitty/kitty/current-theme.conf
